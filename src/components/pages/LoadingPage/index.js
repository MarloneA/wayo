import React from "react";

const LoadingPage = () => (
  <p
    style={{
      display: "flex",
      width: "100vw",
      height: "100vh",
      justifyContent: "center",
      alignItems: "center",
      fontSize: "1rem"
    }}
  >
    ... Loading
  </p>
);

export default LoadingPage;
